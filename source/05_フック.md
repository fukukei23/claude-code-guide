# 05 フック — イベント駆動の自動実行

## フックとは

フックは **特定のイベント発生時に自動実行されるシェルスクリプト**。Claude Codeの動作を拡張・制御する。

```
ユーザーがツールを実行
  │
  ▼
PreToolUse フックが発火 ──→ チェックOK → ツール実行
  │                          └─ チェックNG → ブロック
  ▼
ツールが実行される
  │
  ▼
PostToolUse フックが発火 ──→ ログ記録・後処理
```

---

## 4種のフック

### <a id="sessionstart"></a>SessionStart

**セッション開始時**に実行。

| 項目 | 説明 |
|---|---|
| タイミング | Claude Code起動時 |
| 用途 | 環境変数の読み込み、初期化処理、設定の検証 |
| あなたの設定 | `~/.secrets.env` からAPIキーを注入、MCP設定の自動チェック |

### <a id="pretooluse"></a>PreToolUse

**ツール実行前**に実行。最も重要なフック。

| 項目 | 説明 |
|---|---|
| タイミング | 各ツール（Read, Edit, Bash等）の実行前 |
| 用途 | セキュリティチェック、危険コマンドのブロック、入力検証 |
| あなたの設定 | `check-command-safety.py` が危険コマンドを自動ブロック |
| ブロック可能 | **Yes** — スクリプトがエラーを返すとツール実行がキャンセルされる |

> **現場の知見**: フックのスクリプトがエラー（exit code ≠ 0）を返すと、ツールの実行がサイレントにブロックされる。意図せぬブロックに注意。→ [11_現場の知見](11_現場の知見.md#b-落とし穴ハマりポイント)

ブロックされるコマンドの例:
- `bash -x`（デバッグ出力でシークレット露出の恐れ）
- `cat -A`（特殊文字表示でシークレット露出の恐れ）
- マスクなしの `grep`（APIキーを含むファイルの検索）
- フィルタなしの `env`（全環境変数の表示）

### <a id="posttooluse"></a>PostToolUse

**ツール実行後**に実行。

| 項目 | 説明 |
|---|---|
| タイミング | 各ツールの実行後 |
| 用途 | ログ記録、実行結果の後処理、通知 |
| あなたの設定 | MCP設定変更時の使い分けガイド自動更新 |

### <a id="sessionend"></a>SessionEnd

**セッション終了時**に実行。

| 項目 | 説明 |
|---|---|
| タイミング | Claude Code終了時 |
| 用途 | クリーンアップ、ログ保存、ハンドオフ情報の書き出し |
| あなたの設定 | セッション終了時のサマリー記録 |

---

## 設定方法

フックは `settings.json` の `hooks` セクションに定義する。

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ~/.claude/scripts/security/check-command-safety.py"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "source ~/.secrets.env"
          }
        ]
      }
    ]
  }
}
```

### matcherについて

- `matcher` はツール名を指定（例: `"Bash"`, `"Edit"`, `"Read"`）
- 空文字 `""` は全ツールにマッチ
- 複数のフックを設定可能（配列で定義）

---

## あなたの現在のフック構成

| フック | スクリプト | 役割 |
|---|---|---|
| SessionStart | `source ~/.secrets.env` | APIキー注入 |
| SessionStart | MCP設定チェック | ガイドとの差分検知 |
| PreToolUse (Bash) | `check-command-safety.py` | 危険コマンドのブロック |
| SessionEnd | ハンドオフ記録 | セッション終了時の情報保存 |

---

## デバッグ方法

フックが期待通りに動かない場合:

1. **フックのログを確認** — エラーメッセージが表示される
2. **手動でスクリプトを実行** — ターミナルで直接実行して動作確認
3. **`/config` で設定確認** — hooks セクションの内容を確認
4. **一時的に無効化** — settings.json で該当フックをコメントアウト

---

## 次の章

- **[06_メモリ](06_メモリ.md)** — メモリシステム
- **[00_早見表](00_早見表.md)** — 全機能のチートシートに戻る

## 関連

- [Claude-Code完全ガイド作成](../../01_DECISIONS/claude-code/2026-05-20_Claude-Code完全ガイド作成.md) — ガイド全体の設計と作成
- [Hook-SSOT移行](../../01_DECISIONS/claude-code/2026-04-27_Hook-SSOT移行.md) — Hooks設定のSSOT管理への移行
